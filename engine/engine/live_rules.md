# AstroTradeRB — Live Activation Rules

LIVE mode is allowed ONLY if ALL are true:
- Backtest confidence ≥ 70%
- Simulator exit criteria passed
- No active guardrail locks
- Manual confirmation by user

If any rule fails:
→ Mode remains PAPER
